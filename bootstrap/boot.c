/**
 * Code to support the boot process (displaying the boot screen, loading the first program, etc.)
 */

#include <stdio.h>

#include "boot.h"

/**
 * Display the boot screen
 */
void boot_screen() {
    printf("This is a test of the Toolbox as a linkable library...\n");
}
